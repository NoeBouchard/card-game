import java.util.ArrayList;
public class Pack {
    ///pack is an array of cards (8*n cards total, n is number of players)
    ///We chose the highest card value to also be equal to 8n and a minimum 

    public ArrayList<Integer> makePack(int numberOfCards) {
        ArrayList<Integer> pack = new ArrayList<Integer>();
        for (int i = 1; i <= numberOfCards; i++) {
        }
        return pack;
    }
}
